[workspace]
resolver = "2"
members = [
    "crates/ballistics-core",
    "crates/ballistics-cli",
    "crates/ballistics-wasm",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
